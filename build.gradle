subprojects {
    apply plugin: 'idea'

//    compileJava.options.compilerArgs = ['-Xlint:unchecked']

    repositories {
        jcenter()
    }

    dependencies {
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.6'
}

