<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:id="@+id/incid_see_usercomu_importancia_layout"
              style="@style/baseLayout.withPadding"
              xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_marginTop="@dimen/app_bar_bottom_margin">
    <TextView
        android:id="@+id/incid_incidencias_rot"
        style="@style/textBase.textPageLabel2"
        android:text="@string/incid_incidencia_importancia_rot"/>
    <ListView
        android:id="@id/android:list"
        style="@style/basicList"
        android:choiceMode="none"
        android:listSelector="@color/transparent">
    </ListView>
    <TextView
        android:id="@id/android:empty"
        style="@style/textBase.no_item_list_message"
        android:text="@string/no_importancia_by_user"
        android:visibility="gone">
    </TextView>

</LinearLayout>