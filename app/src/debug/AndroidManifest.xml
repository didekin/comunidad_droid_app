<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.didekindroid"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".DidekinApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/themeOne">

        <!-- ......... -->
        <!-- Notification Listener for tests. -->
        <service
            android:name=".common.gcm.GcmNotificationListenerServ"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

        <activity
            android:name=".common.gcm.GcmBroadCastNotificationActivity"
            android:label="@string/title_activity_gcm_notification"
            android:theme="@style/themeOne">
        </activity>
        <!-- Activity for UIException tests. -->
        <activity
            android:name=".common.activity.MockActivity"
            android:label="@string/mockActivity_label"
            android:theme="@style/themeOne"
            android:launchMode="singleTask">
        </activity>
    </application>

</manifest>
